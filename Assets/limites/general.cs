using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class general : MonoBehaviour
{
    // Start is called before the first frame update
    
    public float multiplicador = 1.2f;

    void Start()
    {
        multiplicador = 1.2f;
    }
    
}
